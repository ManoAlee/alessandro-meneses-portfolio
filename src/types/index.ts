export interface Project {
    id: string;
    title: string;
    description: string;
    link: string;
    github?: string;
}

export interface ComponentProps {
    title: string;
    description: string;
    link: string;
    github?: string;
}